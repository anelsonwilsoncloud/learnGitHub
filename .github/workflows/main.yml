name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Use Greet Action
        uses: ./.github/actions/greet
        with:
          who-to-greet: 'Alina the Octocat'
